# AI Agent Lab 面试讲稿

> 本文件是简历项目「AI Agent Lab」的面试讲稿,按模块拆分,每条结构为:**一句话开场 → 技术决策 → 实现细节 → 可能的追问及回答**。
> 目标:不提示的情况下,每条能流利讲 2-3 分钟。8 条全部讲完约 20 分钟,正好覆盖一个技术面时长。
>
> **使用方法**:不要背。每天挑 2-3 条开口讲,手机录音,回放标记卡顿处。目标是流利、不跑题、能接住追问。

---

## 目录

1. [项目总述](#1-项目总述开场必讲)
2. [Agentic RAG](#2-agentic-rag最高频必被追问)
3. [多工具 Agent](#3-多工具-agent)
4. [双层 Memory](#4-双层-memory)
5. [MCP 协议](#5-mcp-协议)
6. [多 Agent 工作流](#6-多-agent-工作流)
7. [认证与数据隔离](#7-认证与数据隔离)
8. [前端工程化](#8-前端工程化)

---

## 1. 项目总述(开场必讲)

这个项目是我从零搭建的一个企业知识库智能助手,技术栈是 Next.js 16 加 AI SDK 6。它不是一个聊天 demo,而是一个集成度比较高的 AI Agent 应用——包含多工具调用、RAG 知识库检索、跨会话记忆、MCP 协议接入和多 Agent 工作流。后端用 Supabase 的 PostgreSQL 加 pgvector 做向量存储,已经部署上线,支持用户登录和数据隔离。

做这个项目的目标是把 AI Agent 开发的完整链路走一遍,从最基础的流式对话,到工具调用、知识库、记忆系统、协议层,最后到多 Agent 编排,每一块都做到可运行、可解释的生产级实现。

**讲的时候注意**:这一段是电梯演讲,30 秒内讲完。面试官听完应该知道"这个人不只是调了个 API,而是真正理解了整条链路"。

---

## 2. Agentic RAG(最高频,必被追问)

这个项目的核心是 RAG 知识库,但我用的是 Agentic RAG,不是传统 RAG。区别在于:传统 RAG 是每次提问都强制检索,而 Agentic RAG 是把知识库检索做成了一个工具,让 Agent 自己判断这个问题需不需要查知识库。

具体实现上,文档上传时会先做切分。我用的策略是按段落优先切分,段落超过 500 字符再按句子拆,保证语义完整。然后调用阿里云百炼的 text-embedding-v4 模型生成向量,1024 维,存到 PostgreSQL 的 pgvector 里。

检索时用余弦相似度,加了 HNSW 索引,把向量检索从暴力扫描的 O(n) 降到近似对数级。Top-K 设的 4,也就是取最相关的 4 个片段拼进上下文。

一个关键设计是:回答末尾必须标注信息来源,格式是"参考资料:《文档标题》"。如果检索结果为空,系统会如实告诉用户"知识库中暂无相关内容",而不是编造——这是 RAG 和普通聊天最大的区别。

### 可能追问

- **「为什么选 500 字符?」** → 中文按字符计更直观,1024 维 embedding 容纳的语义量大概在这个范围。太小会丢失上下文,太大会稀释相关性。这个值可以调,生产环境要根据实际文档做 A/B 测试。
- **「HNSW 的参数怎么选的?」** → m 设 16、efConstruction 设 64,这是 pgvector 的默认值,适合中小规模数据。数据量到百万级以上可能要调大 efSearch。
- **「检索质量怎么提升?」** → 我了解的有三个方向:加 rerank 用交叉编码器重排、混合检索把向量检索和关键词检索结果融合、query 改写把口语化问题转成更适合检索的关键词。这是后续优化方向。

---

## 3. 多工具 Agent

对话模块集成了 5 个工具:天气查询、数学计算、时间查询、知识库检索、记忆保存。每个工具都是用 AI SDK 的 `tool` 函数定义的,包含 description、inputSchema 和 execute 三部分。模型只能看到前两部分,execute 是服务端私有逻辑。

关键设计是开了多步调用,用 `stepCountIs(5)` 控制循环上限。这意味着模型可以"调工具 → 看结果 → 继续推理 → 再调工具",最多循环 5 步,避免无限循环。没有这个配置,模型只会调一次工具就停。

save_memory 这个工具比较特别——它体现了 Agent 的自主性。模型在对话中判断"这个信息值得长期记住"时,会主动调用 save_memory,把用户透露的事实或偏好写入数据库。这不是我写规则触发的,是 Agent 自己决策的。

### 可能追问

- **「Agent 怎么决定调哪个工具?」** → 靠 description。我写的 description 很具体,会说明"什么场景调用"和"什么场景不调用"。模型根据用户问题和工具描述做语义匹配。
- **「工具执行失败怎么办?」** → execute 函数里做了错误处理,返回带 error 字段的结构化结果,模型拿到后会告诉用户"操作失败"而不是崩溃。比如数学工具会校验表达式合法性,非法字符直接返回错误信息。

---

## 4. 双层 Memory

记忆系统我做了两层,解决的是不同维度的问题。

第一层是短期上下文管理,管的是单次对话内的 token 控制。长对话会爆上下文窗口,我的做法是先剥离旧的工具调用结果(这些结果往往很长但已经不需要了),如果消息还是太长,就把早期对话压缩成摘要。这样既省 token,又不丢失关键信息。

第二层是持久化记忆,管的是跨会话记忆。参考的是 ChatGPT Memory 的设计:不存对话原文,只存 Agent 提取的精简事实和偏好。比如用户说"我在上海工作",Agent 会调用 save_memory 存一条 fact 类型的记忆。下次新对话开始时,系统把用户的所有记忆全量注入 system prompt,Agent 就"记住"了。

两层互补:上下文管理管"这次对话怎么省 token",持久化记忆管"跨多次对话记住用户"。会话结束后短期记忆消失,长期记忆留在数据库。

### 可能追问

- **「为什么不全量存对话?」** → 成本和噪音。对话里大部分内容是寒暄、过程性讨论,不值得存。参考 ChatGPT 的做法,只存精简事实,检索效率高,噪音低。
- **「记忆多了怎么办?」** → 现在记忆条目少,全量注入 system prompt 最简单可靠。如果增长到几百条,会升级成向量检索,只注入和当前问题相关的记忆。这是一个可扩展的设计。
- **「怎么防止存垃圾?」** → 两个机制:save_memory 的 description 明确写了"不要存临时信息、寒暄";content 字段限制了 200 字,强制精简。

---

## 5. MCP 协议

MCP 是 Model Context Protocol,我实现了完整的 Server 端和 Client 端。

Server 是一个独立进程,用 Model Context Protocol SDK 创建,通过 stdio 传输和 Client 通信。它暴露了三个原语:Tool 是 Agent 主动调用的函数,比如搜索知识库;Resource 是被动暴露的只读数据,比如文档清单;Prompt 是可复用的提示词模板。

Client 在 Next.js 的 Route Handler 里,用 AI SDK 的 createMCPClient 连接 Server,拿到 Server 暴露的工具,注入到对话流里。

为什么值得单独做:MCP 的核心价值是标准化。传统做法是把工具写死在应用代码里,换个宿主就得重写。MCP 让工具定义和宿主解耦——我这个 Server 不光能被自己的应用调用,Claude Desktop、Cursor 这些 MCP Client 都能消费同一套工具。

### 可能追问

- **「Tool 和 Resource 有什么区别?」** → Tool 是主动的,Agent 决定调用;Resource 是被动的,Client 按需读取。Tool 是动作,Resource 是数据。
- **「stdio 传输有什么局限?」** → 只能本地进程间通信。远程场景要用 SSE 或 HTTP 传输,但 stdio 最简单,适合本地开发和学习协议。
- **「每次请求都新建连接,性能怎么样?」** → 对,现在是每次请求创建 Client 再关闭,为了演示清晰。生产环境应该复用连接池,减少握手开销。

---

## 6. 多 Agent 工作流

我做了一个内容创作工作流,三个 Agent 串联协作:规划 Agent、研究 Agent、写作 Agent。

规划 Agent 用 Structured Output 把模糊的创作需求拆解成结构化计划——主题、大纲、语气、字数要求。研究 Agent 拿到计划后,检索知识库收集相关资料。写作 Agent 汇总计划和资料,生成最终内容。

技术上,每个 Agent 是一次独立的 generateText 或 streamText 调用,前一个的输出作为后一个的输入。整个流程通过流式输出实时展示每一步的进度,用户能看到"正在规划→正在研究→正在写作"。

这个模式在真实业务里很常见:客服分级处理、数据分析报告生成、自动化内容审核,本质都是多 Agent 串联。

### 可能追问

- **「为什么不用一个 Agent 做完所有事?」** → 单 Agent 的 prompt 会很长,职责不清晰,而且容易遗忘指令。拆成多个 Agent,每个有明确的角色和 system prompt,效果更好。这叫 prompt 工程的分治思想。
- **「Agent 之间怎么传递数据?」** → 用 Structured Output 保证数据格式。规划 Agent 输出的是结构化对象,研究 Agent 直接用这个对象的字段去检索,不用解析自然语言。

---

## 7. 认证与数据隔离

认证用的是 Supabase SSR Auth,适配 Next.js App Router 的 SSR 模式。

核心是双 Client 架构:createServerClient 在服务端用,从请求的 cookie 里读 session;createBrowserClient 在浏览器端用,自动管理 document.cookie。

Session 用的是 JWT,access_token 有效期短,默认 1 小时。我在 Next.js 16 的 proxy 层(就是以前的 middleware)做了 token 静默刷新:每个请求都跑一次 getUser(),如果 token 快过期了,Supabase 会返回新 token,proxy 把它写回 cookie,用户完全无感。

Route Handler 里用 getUser() 做鉴权,拿到 user.id 注入到工具集和数据库查询里,实现记忆按用户隔离。未登录请求直接返回 401。

### 可能追问

- **「proxy 为什么不做路由守卫和重定向?」** → proxy 对每个请求都会执行,包括预取,所以只做轻量的 session 续期。真正的授权判断放数据层,用 getUser() 验证。这是官方推荐的安全模式。
- **「token 过期但用户在用,会突然掉线吗?」** → 不会。proxy 静默刷新,用户无感。只有 refresh_token 也过期了(默认比较长)才会要求重新登录。
- **「anon key 暴露在前端安全吗?」** → 安全。anon key 配合 RLS 行级安全策略,即使拿到 key 也只能访问该用户自己的数据。service_role key 才是敏感的,绝不放前端。

---

## 8. 前端工程化

前端数据层用 TanStack Query 管理,替代了原来手写的 fetch + useState + useEffect 样板。

具体改造的是知识库管理页面。列表查询用 useQuery,自动获得 loading/error 状态、缓存、请求去重。上传用 useMutation,成功后 invalidateQueries 自动刷新列表。删除做了乐观更新:onMutate 里先从缓存移除文档,UI 立即变化,如果 API 失败在 onError 里回滚。

全局用 QueryProvider 包裹,QueryClient 用 useState 创建——这是为了防止 Next.js 模块级单例在请求间串数据。

### 可能追问

- **「为什么用 TanStack Query 而不是 Redux?」** → Redux 管客户端状态,TanStack Query 专管服务端状态缓存。两者不冲突,但这个项目没有复杂的客户端状态,Query 一个就够了。
- **「乐观更新怎么回滚的?」** → onMutate 返回之前的缓存快照,onError 时收到这个快照,调 setQueryData 还原。TanStack Query 自动管理这个上下文传递。
- **「staleTime 和 gcTime 的区别?」** → staleTime 是数据多久算"旧",旧了会后台静默刷新但 UI 先用旧数据;gcTime 是没人用之后多久从内存删掉。面试常考这俩概念的区别。

---

## 练习建议

1. 每天挑 2-3 条,**开口讲**,用手机录音
2. 回放,标记哪里卡顿、哪里讲得太长、哪里没说清楚
3. 第二天换另外几条重复,同时回看昨天的
4. 目标:不提示的情况下,每条能流利讲 2-3 分钟,不卡顿,不跑题
5. 大概一周左右就能全部讲顺

8 条全部讲完大约 20 分钟,正好是一个技术面的正常时长。面试官不会让你全讲,他会被其中某条吸引然后深入追问——追问的部分,每条的「可能追问」就是预案。
